# Core dependencies
pandas>=1.5.0

# PDF extraction (at least one required)
pypdf>=3.0.0
# Alternative: PyPDF2>=2.0.0

# Optional: for better PDF extraction
# Install system package: poppler-utils (provides pdftotext)
