# Database Scripts (Bibliotheque Maison)

This directory mirrors SQL schema, view, and migration scripts extracted from
`/home/ruuuzer/Documents/monprojet/db`.

Contents:
- `01_schema.sql`, `02_restructuration_tables.sql`, `02_views.sql`, `init.sql`
- `migrations/` for incremental changes
- backup snapshots (`epicerie_backup_*.sql`)

Notes:
- These SQL files are copied for standalone reuse.
- Update sources in `/home/ruuuzer/Documents/monprojet/db` and re-sync here if needed.
