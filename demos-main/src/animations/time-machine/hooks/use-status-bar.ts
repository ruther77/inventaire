import type { SharedValue } from 'react-native-reanimated';

export const useStatusBar = (_timeMachineProgress: SharedValue<number>) => {
  // StatusBar management removed - handled by main app layout
};
